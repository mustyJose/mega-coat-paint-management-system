# Paint Store Management System

An offline management system for paint stores, built with React, TypeScript, Node.js, Express, and SQLite.

## Project Structure

- `backend` - REST API and business logic
- `frontend` - User interface

## Status

Project setup in progress.